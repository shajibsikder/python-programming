import math


def Tarea():
 import math
A = float(input("Enter:"))
B = float(input("Enter:"))
C = float(input("Enter:"))

if(A+B>C)and(B+C>A)and(C+A>B):
    S = ((A+B+C)/2)
    Area = math.sqrt(S*(S-A)*(S-B)*(S-C))
    print("Area",Area)
Tarea()
